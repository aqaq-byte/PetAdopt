<!DOCTYPE html>
<html lang="en">

<head>
    <title><%= title %></title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="style.css">
    <script src="script.js"></script>
</head>

<body>
<%- include("./header") %>
<%- include("./sidebar") %>

    <div id="content" style="padding: 3%;">
        <p style="color: red;"><%= message %></p>
        <% if (pageType === "registration page") { %>
            <a href="/register">← Go back to <%= pageType %></a>
        <% } else if (pageType === "login page") { %>
            <a href="/login">← Go back to <%= pageType %></a>
        <% } %>
        </form>
    </div>

    <%- include("./footer") %>

</body>

</html>

